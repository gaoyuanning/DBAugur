1,"select top # x, y, c=sin(y+x)  from data where y < # -- negative   and cos(x)-y*y <# order by RunId asc, x desc */   select RunID, N=count(ID), Nr=count(distinct RunID), AVG(Y) as AvgY from data where X># or X<# group by RunID"
2019-04-24 12:45:00,1
