1,"select top # x, y, c=sin(y+x) from data where y < #   and cos(x)-y*y > # order by RunID asc, x desc */  /* select RunID, N=count(ID), Nr=count(distinct RunID), AVG(Y) as AvgY from data where X > # or X < # group by RunID */    select x=round(x,#), n=count(ID) from data group by round(x,#) order by #"
2019-04-24 12:49:00,1
