1,"select top # x,y,c=sin(y+x) from data where y<# -- negative   and cos(x)-y*y<# order by x desc */       select RunID, N=count(ID), Nr=count(distinct RUNID), AVG(Y) as AvgY from data where X># or X<# group by RunID   having AVG(Y)># order by RunID desc"
2019-04-24 12:47:00,1
