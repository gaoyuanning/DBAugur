1,"select top # x, y, c = sin(x+y) from data  where y < # -- negative    and cos(x) - y*y < # order by x desc        select RunID, N = count (ID), Nr = count (distinct RunID), AVG(Y) as AvgY from data where x > # or x < # group by RunID"
2019-04-24 12:45:00,1
