1,"select RunID, N= count(ID), Nr=count(distinct(RunID)), AVG(Y) as AvgY from data where X > # or X < # group by RunID having AVG(Y) > # order by RunID desc */       select x = round(x,#), n = count(ID) from data group by round(x,#) order by #"
2019-04-24 12:49:00,1
