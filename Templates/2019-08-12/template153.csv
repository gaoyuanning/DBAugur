1,"SELECT TOP # p.ra,p.dec,p.run,p.rerun,p.camCol,p.field,p.obj, q.fieldQall as quality FROM BESTDR7..PhotoObj AS p 	JOIN RunQA AS q ON q.fieldid = p.fieldid 	JOIN #x AS x ON x.objID=p.objID JOIN #upload AS u ON u.up_id = x.up_id  WHERE  ( p.type = # OR p.type = # OR p.type = #) ORDER BY x.up_id"
2019-08-12 19:25:00,1
