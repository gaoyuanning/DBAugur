6,"SELECT count(*) as &&&,   sum( case when (s.z >= # and s.z < #) then # else # end) as &&&,   sum( case when (s.z >= # and s.z < #) then # else # end) as &&&,  sum( case when (s.z >= # and s.z < #) then # else # end) as &&&,   sum( case when (s.z >= # and s.z < #) then # else # end) as &&&,  sum( case when (s.z >= # and s.z < #) then # else # end) as &&&,   sum( case when (s.z >= #) then # else # end) as &&& FROM WISE_xmatch as xm   JOIN PhotoObj AS p on xm.sdss_objid = p.objid  JOIN SpecObj AS s on p.objid = s.bestobjid  JOIN WISE_allsky AS w on xm.wise_cntr = w.cntr WHERE  s.class=&&& and (s.subclass not like &&& and s.subclass not like &&& and s.subclass not like &&& and s.subclass not like &&& )"
2019-09-12 09:53:00,1
2019-09-12 09:55:00,1
2019-09-12 09:56:00,1
2019-09-12 09:57:00,2
2019-09-12 09:58:00,1
