1,"select fieldID,ra,dec,Run.strip, Run.run, Run.rerun, field, camcol, raMin, raMax, decMin, decMax from Field inner join dbo.Run on Run.run=Field.run where Run.stripe=# and ra between # and # --select top # * from dbo.PhotoObjAll where type=# and field=# and run=# and camcol=# select top # * from dbo.specObj where class=&&& and tile=#"
2019-01-29 05:29:00,1
