1,"select county, C=centroid.ToString(), SRID=centroid.STSrid, dist=centroid.STDistance(geography::Point(#,#, #))     from optw.tamass.TRACTS     where state=#     order by # desc"
2019-10-26 16:53:00,1
