1,"SELECT TOP # albumId,milliseconds           FROM chinook.Track          ORDER BY milliseconds ) SELECT a.title,t.name as track, t.milliseconds,st.milliseconds as shortest   FROM st    inner join chinook.album a       on a.albumid=st.albumid    inner join chinook.track t       on t.albumid=a.albumid"
2019-03-31 17:12:00,1
