1,"SELECT a.title,t.milliseconds,st.milliseconds   FROM (SELECT TOP # albumId,milliseconds           FROM chinook.Track          ORDER BY milliseconds) st    inner join chinook.album a       on a.albumid=st.albumid    inner join chinook.track t       on t.albumid=a.albumid"
2019-03-31 17:10:00,1
