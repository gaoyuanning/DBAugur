1,"SELECT a.title,t.name as track, t.milliseconds,st.milliseconds as shortest   FROM (SELECT TOP # albumId,milliseconds           FROM chinook.Track          ORDER BY milliseconds) st    inner join chinook.album a       on a.albumid=st.albumid    inner join chinook.track t       on t.albumid=a.albumid"
2019-04-01 16:06:00,1
