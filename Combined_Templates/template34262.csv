1,"select RunID, N = count (ID) , Nr = count (distinct (RunID) ), AVG (Y) as AvgY from data where X > # or X < # group by RunID having AVG (Y) > # order by RunID desc */ -- histogramming trick declare @bin float = # select x = round (x / @bin, #) * @bin, n = count (ID) from data group by round (x / @bin, #) * @bin order by #"
2019-04-24 12:52:00,1
